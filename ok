Approach for RODAN and PlainID Integration: The plan is to fetch the resource /ppm/PPM/Administrator/Resource from RODAN, along with its group, action, and deny status. I'll develop a method to extract this data, ensuring only allowed actions (e.g., view) are processed. Next, I will create resource types and resources in the PlainID Asset Store, grouping them logically by application or functionality (e.g., Administrator). For each group-action-resource combination, a policy will be created in PlainID (e.g., abs-it-developers-view-Access). Afterward, the resources will be linked to these policies, enforcing the access control. Finally, I will test the integration by verifying that the resources appear in the Asset Store and are correctly linked to policies, ensuring the proper access permissions are applied based on RODAN entitlements. Let me know if this approach is aligned with expectations or if any changes are required.
