
def create_application_set_in_asset_store(application_set_name, cmdb_application_id):

    payload = {
        "applicationSet": application_set_name,
        "applicationName": application_set_name,  # Assuming this is same as applicationSet
        "cmdbApplicationId": cmdb_application_id
    }

    _log.info(f"Attempting to create application set at {asset_store_api_url}")
    _log.info(f"Payload: {json.dumps(payload, indent=2)}")
    
    try:
        response = requests.post(asset_store_api_url, data=json.dumps(payload), headers=headers,
                                 auth=(asset_store_user, asset_store_password))
        if response.status_code == 200 or response.status_code == 201:
            _log.info(f"Application set {application_set_name} created successfully.")
            return response.json()
        else:
            _log.error(f"Failed to create ApplicationSet for {application_set_name}. Status: {response.status_code}, "
                       f"Response: {response.text}")
            return {}
    except Exception as e:
        _log.error(f"Error creating ApplicationSet: {str(e)}")
        return {}



from cmdb_application import get_cmdb_application_by_id
from policies import create_application_set_in_asset_store
import logging


logging.basicConfig(level=logging.INFO)
_log = logging.getLogger(__name__)

def migrate_cmdb_application_to_asset_store(application_name):

    # Fetch the CMDB application by name
    cmdb_app = get_cmdb_application_by_id(application_name)
    
    if cmdb_app:
        cmdb_application_id = cmdb_app["cmdb_application_id"]
        
        # Create Application Set in Asset Store
        application_set_response = create_application_set_in_asset_store(application_name, cmdb_application_id)
        
        if "applicationSet" in application_set_response:
            _log.info(f"Application set created successfully for {application_name}")
        else:
            _log.error(f"Failed to create Application Set for {application_name}")
    else:
        _log.error(f"CMDB application {application_name} not found.")

if __name__ == "__main__":
    application_name = "Test Set for PPM"
    migrate_cmdb_application_to_asset_store(application_name)
