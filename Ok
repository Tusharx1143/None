Yes, the modified code in `policies.py` is designed to handle the integration with **RODAN** by automating the creation of policies, resources, and linking those resources in PlainID based on the **RODAN entitlements**. Here's how it specifically addresses RODAN-related tasks:

### How the Code Integrates with RODAN:

1. **Fetching Entitlements from RODAN**:
   - The function `create_policies_based_on_resource` retrieves entitlements from RODAN using the `entitlements.get_entitlements` method.
   - The `resource` and `action_filter` parameters passed to this function are used to query RODAN for the relevant entitlements.
   - These entitlements contain information about users/groups and the actions they are allowed to perform on RODAN resources.

2. **Filtering RODAN Entitlements**:
   - The code filters the RODAN entitlements to identify groups and actions that need policies. It specifically filters out:
     - User-based entitlements by ensuring that the `group` key is present.
     - Deny entitlements, ensuring only allow/permit actions are processed (checks for `"deny": "false"` in the entitlement).

3. **Creating Dynamic Groups for RODAN Users/Groups**:
   - Although the current code leaves the `create` function for groups commented out, it is structured to dynamically create groups based on the `rodan_groups` set.
   - These dynamic groups will represent the users or groups in RODAN that have access to certain resources. The groups are then linked to policies for access control in PlainID.

4. **Creating Policies for RODAN Actions**:
   - The code creates a policy in PlainID for each action that is associated with the RODAN entitlements. These actions could be things like "read", "write", "delete" — any action defined in RODAN that needs to be mirrored in PlainID.
   - For each action, the policy is named using the pattern `"<application>-<action>-Access"`, ensuring that policies are clearly tied to specific actions in RODAN.

5. **Creating Resource Types and Resources**:
   - The function calls `create_resource_type` and `create_resource` from `resources.py` to create resource types and individual resources in the PlainID Asset Store.
   - These resources represent the actual RODAN resources (such as applications, files, or data sets) that users or groups are allowed to access based on their entitlements.

6. **Linking RODAN Resources to Policies**:
   - Once the resource types and resources are created in the PlainID Asset Store, the code links these resources to the corresponding policies using the `link_resources_to_policy` function.
   - This ensures that the RODAN resources are properly controlled by the access policies created in PlainID, enforcing the access controls defined in RODAN.

### **Summary of How It Works for RODAN**:

1. **Fetches Entitlements**: The code retrieves the RODAN entitlements for a given resource and action filter.
2. **Creates Groups**: It identifies the groups from RODAN that have access to the resources and can create corresponding dynamic groups in PlainID (currently commented out but structured for implementation).
3. **Creates Policies**: Policies are created in PlainID based on the RODAN actions (e.g., "read", "write", "delete") that users/groups are allowed to perform on RODAN resources.
4. **Creates Resources**: The code creates resource types and resources in the PlainID Asset Store that correspond to RODAN resources.
5. **Links Resources to Policies**: Finally, the RODAN resources are linked to the policies, ensuring access controls are properly enforced in PlainID, based on the RODAN configuration.

### **Testing for RODAN**:
When testing the code for RODAN, ensure that:
1. **RODAN Entitlements** are fetched and processed correctly (i.e., the right users/groups and actions are identified).
2. **Resources in RODAN** are reflected as resources in the PlainID Asset Store.
3. **Actions defined in RODAN** match the actions created in PlainID, and all actions required for the resource are properly linked to the policies.
4. **Groups from RODAN** are correctly mapped to dynamic groups in PlainID (if implemented).

The code, as structured, automates the policy and resource creation based on RODAN entitlements and integrates these with the PlainID access control system. If you want to make further modifications or enable the commented-out sections (such as group creation), you can extend the functionality for full dynamic group management.






import json
import logging
import requests
from http import HTTPStatus
import config
import namespace
import cmdb_application
import entitlements
# Import from resources.py to create resources in the Asset Store
from resources import create_resource_type, create_resource

# Headers and API configuration
headers = {"Content-type": "application/json"}

_log = logging.getLogger('config')

base_url = config.get_arg("plainid.url")
api_url = base_url + "/rest/permission"
plainid_user = config.get_arg("plainid.user")
plainid_password = config.get_arg("plainid.password")

asset_store_base_url = config.get_arg("assetstore.url")
asset_store_api_url = asset_store_base_url + "/api/v1/application-sets"  # version: v1
asset_store_user = config.get_arg("assetstore.user")
asset_store_password = config.get_arg("assetstore.password")

# ============================
# New Function: link_resources_to_policy
# Purpose: Links resources created in the Asset Store to the policies in PlainID
# ============================
def link_resources_to_policy(policy_id, resource_ids):
    """
    Link resources to a given policy.
    
    Args:
    - policy_id (str): The ID of the policy.
    - resource_ids (list): List of resource IDs to link to the policy.
    
    Returns:
    - None
    """
    data = {
        "resources": resource_ids
    }

    try:
        url = f"{api_url}/{policy_id}/resources"
        response = requests.post(url=url, data=json.dumps(data), headers=headers, auth=(plainid_user, plainid_password))
        if response.status_code == HTTPStatus.OK:
            _log.info("Resources linked to policy successfully")
        else:
            _log.debug(f"Unexpected response code from REST call: {response.status_code}")
            raise Exception(response.text)
    except Exception as error:
        _log.error("Error linking resources to policy: %s %s", str(error), sys.exc_info())
        raise error

# ============================
# Function: create_policies_based_on_resource
# Changes: Enhanced to create resource types and resources, and link them to policies
# ============================
def create_policies_based_on_resource(application, resource, action_filter):
    """
    Create policies based on a given resource and action filter.
    
    Args:
    - application (str): The name of the application.
    - resource (str): The resource for which to create policies.
    - action_filter (str): Filter for specific actions.
    
    Returns:
    - bool: True if policies are created successfully.
    """
    # Pre-flight checks
    cmdb_app = cmdb_application.get_cmdb_application(application)
    if not cmdb_app:
        raise Exception(f"Application {application} not found in CMDB!")
    application_sys_id = cmdb_app['sys_id']
    ns = namespace.get_namespace_or_throw(application)
    if ns is not None:
        namespace_id = ns['id']

    # Fetch entitlements from RODAN using resource string
    try:
        entitlements_list = entitlements.get_entitlements(resource, action_filter)['entitlements']['entitlement']
    except Exception as error:
        raise Exception(f"Error while fetching entitlements for resource: {resource}")

    # Set of actions and groups
    rodan_actions = set()
    rodan_groups = set()

    for entitlement in entitlements_list:
        if "group" in entitlement and entitlement["deny"] == "false":
            rodan_actions.add(entitlement["action"])
            rodan_groups.add(entitlement["group"])

    if len(rodan_actions) == 0:
        raise Exception("No actions found in entitlements")
    if len(rodan_groups) == 0:
        raise Exception("No groups found in entitlements")

    # Get the master list of actions from the asset store
    asset_store_action_list = action.get_asset_store_actions()
    if len(asset_store_action_list) == 0:
        raise Exception("No actions found in asset store")

    # Verify all actions exist in asset store and PlainID
    for action_name in sorted(rodan_actions):
        if action_name not in asset_store_action_list:
            raise Exception(f"Action {action_name} is missing from Asset store")
        plainid_action = get_action_by_name(action_name)
        if plainid_action is None:
            raise Exception(f"Action {action_name} is missing from PlainID")

    # ============================
    # Create dynamic groups into global namespace (existing functionality)
    # This part is commented out for now but ready to handle dynamic group creation.
    # ============================
    groups_dict = {}
    # for group_name in rodan_groups:
    #     dynamic_group_response = groups.create(group_type="swarm", group_name=group_name, group_description=group_name)
    #     groups_dict.update({group_name: dynamic_group_response['id']})

    # ============================
    # Creating Policies and Linking Resources
    # New code below: This section creates resources and links them to policies.
    # ============================
    actions_dict = {}
    try:
        for action_name in sorted(rodan_actions):
            policy_name = f"{application}-{action_name}-Access"
            description = policy_name
            policy = create_policy(namespace_id, policy_name, description)
            actions_dict.update({action_name: policy['id']})

            # ============================
            # New Code: Create resource type and resource in Asset Store
            # Purpose: Ensures that RODAN resources are represented in the PlainID Asset Store
            # ============================
            resource_type_response = create_resource_type("RODAN", application_sys_id)
            resource_type_id = resource_type_response['resource_type_application_set_key']
            resource_response = create_resource(application_sys_id, resource_type_id, resource)
            resource_id = resource_response['resource_map_key']

            # ============================
            # New Code: Link the created resources to the policies
            # Purpose: Links the created resources to the policies to ensure access control
            # ============================
            link_resources_to_policy(policy['id'], [resource_id])

            _log.info(f"Policy {policy_name} created and resource {resource} linked successfully.")
    except Exception as error:
        raise Exception(f"Error while creating policy and linking resources: {str(error)}")

    # Link dynamic groups to policy (if needed in the future)
    for entitlement in entitlements_list:
        if "group" in entitlement and entitlement["deny"] == "false":
            current_group = groups_dict[entitlement["group"]]
            current_policy = actions_dict[entitlement["action"]]

            data = [current_group]
            dynamic_group_url = f"{api_url}/{current_policy}/permissionRule/dynamic"
            headers.update({"namespace": namespace_id})

            _log.info(f"Link the dynamic group {data} in the policy {current_policy}")
            try:
                r = requests.post(url=dynamic_group_url, data=json.dumps(data), auth=(plainid_user, plainid_password), headers=headers)
                if r.status_code == HTTPStatus.OK:
                    _log.info("Linked the dynamic group to the policy successfully")
                else:
                    _log.debug(f"Unexpected response code from REST call: {r.status_code}")
            except Exception as error:
                _log.error("Error linking dynamic group to policy: %s %s", str(error), sys.exc_info())
                raise error

    return True

# Utility Functions
# These functions were not changed, but they are key to the policy creation flow.
def get_resource_id_by_name(resource_name):
    """
    Retrieve resource ID by resource name.
    
    Args:
    - resource_name (str): The name of the resource.
    
    Returns:
    - str: The resource ID.
    """
    resource_obj = db_objects.get_object_by_name(resource_name, DYNAMIC_LIST_OBJ, base_url,
                                                 auth=(plainid_user, plainid_password))
    return resource_obj['id']

def get_action_by_name(action_name):
    """
    Retrieve action ID by action name.
    
    Args:
    - action_name (str): The name of the action.
    
    Returns:
    - dict: The action object.
    """
    action = db_objects.get_object_by_name(action_name, ACTION_OBJ, base_url,
                                           auth=(plainid_user, plainid_password))
    return action




Certainly! Below is the **highlighted version of the `policies.py` code**, with the key changes marked. I’ve added comments to point out the **new additions** and **modifications** that were made to ensure proper integration between RODAN, the PlainID policies, and the Asset Store.

### Full Modified `policies.py` with Highlighted Changes:

```python
import json
import logging
import requests
from http import HTTPStatus
import config
import namespace
import cmdb_application
import entitlements
# Import from resources.py to create resources in the Asset Store
from resources import create_resource_type, create_resource

# Headers and API configuration
headers = {"Content-type": "application/json"}

_log = logging.getLogger('config')

base_url = config.get_arg("plainid.url")
api_url = base_url + "/rest/permission"
plainid_user = config.get_arg("plainid.user")
plainid_password = config.get_arg("plainid.password")

asset_store_base_url = config.get_arg("assetstore.url")
asset_store_api_url = asset_store_base_url + "/api/v1/application-sets"  # version: v1
asset_store_user = config.get_arg("assetstore.user")
asset_store_password = config.get_arg("assetstore.password")

# ============================
# New Function: link_resources_to_policy
# Purpose: Links resources created in the Asset Store to the policies in PlainID
# ============================
def link_resources_to_policy(policy_id, resource_ids):
    """
    Link resources to a given policy.
    
    Args:
    - policy_id (str): The ID of the policy.
    - resource_ids (list): List of resource IDs to link to the policy.
    
    Returns:
    - None
    """
    data = {
        "resources": resource_ids
    }

    try:
        url = f"{api_url}/{policy_id}/resources"
        response = requests.post(url=url, data=json.dumps(data), headers=headers, auth=(plainid_user, plainid_password))
        if response.status_code == HTTPStatus.OK:
            _log.info("Resources linked to policy successfully")
        else:
            _log.debug(f"Unexpected response code from REST call: {response.status_code}")
            raise Exception(response.text)
    except Exception as error:
        _log.error("Error linking resources to policy: %s %s", str(error), sys.exc_info())
        raise error

# ============================
# Function: create_policies_based_on_resource
# Changes: Enhanced to create resource types and resources, and link them to policies
# ============================
def create_policies_based_on_resource(application, resource, action_filter):
    """
    Create policies based on a given resource and action filter.
    
    Args:
    - application (str): The name of the application.
    - resource (str): The resource for which to create policies.
    - action_filter (str): Filter for specific actions.
    
    Returns:
    - bool: True if policies are created successfully.
    """
    # Pre-flight checks
    cmdb_app = cmdb_application.get_cmdb_application(application)
    if not cmdb_app:
        raise Exception(f"Application {application} not found in CMDB!")
    application_sys_id = cmdb_app['sys_id']
    ns = namespace.get_namespace_or_throw(application)
    if ns is not None:
        namespace_id = ns['id']

    # Fetch entitlements from RODAN using resource string
    try:
        entitlements_list = entitlements.get_entitlements(resource, action_filter)['entitlements']['entitlement']
    except Exception as error:
        raise Exception(f"Error while fetching entitlements for resource: {resource}")

    # Set of actions and groups
    rodan_actions = set()
    rodan_groups = set()

    for entitlement in entitlements_list:
        if "group" in entitlement and entitlement["deny"] == "false":
            rodan_actions.add(entitlement["action"])
            rodan_groups.add(entitlement["group"])

    if len(rodan_actions) == 0:
        raise Exception("No actions found in entitlements")
    if len(rodan_groups) == 0:
        raise Exception("No groups found in entitlements")

    # Get the master list of actions from the asset store
    asset_store_action_list = action.get_asset_store_actions()
    if len(asset_store_action_list) == 0:
        raise Exception("No actions found in asset store")

    # Verify all actions exist in asset store and PlainID
    for action_name in sorted(rodan_actions):
        if action_name not in asset_store_action_list:
            raise Exception(f"Action {action_name} is missing from Asset store")
        plainid_action = get_action_by_name(action_name)
        if plainid_action is None:
            raise Exception(f"Action {action_name} is missing from PlainID")

    # ============================
    # Create dynamic groups into global namespace (existing functionality)
    # This part is commented out for now but ready to handle dynamic group creation.
    # ============================
    groups_dict = {}
    # for group_name in rodan_groups:
    #     dynamic_group_response = groups.create(group_type="swarm", group_name=group_name, group_description=group_name)
    #     groups_dict.update({group_name: dynamic_group_response['id']})

    # ============================
    # Creating Policies and Linking Resources
    # New code below: This section creates resources and links them to policies.
    # ============================
    actions_dict = {}
    try:
        for action_name in sorted(rodan_actions):
            policy_name = f"{application}-{action_name}-Access"
            description = policy_name
            policy = create_policy(namespace_id, policy_name, description)
            actions_dict.update({action_name: policy['id']})

            # ============================
            # New Code: Create resource type and resource in Asset Store
            # Purpose: Ensures that RODAN resources are represented in the PlainID Asset Store
            # ============================
            resource_type_response = create_resource_type("RODAN", application_sys_id)
            resource_type_id = resource_type_response['resource_type_application_set_key']
            resource_response = create_resource(application_sys_id, resource_type_id, resource)
            resource_id = resource_response['resource_map_key']

            # ============================
            # New Code: Link the created resources to the policies
            # Purpose: Links the created resources to the policies to ensure access control
            # ============================
            link_resources_to_policy(policy['id'], [resource_id])

            _log.info(f"Policy {policy_name} created and resource {resource} linked successfully.")
    except Exception as error:
        raise Exception(f"Error while creating policy and linking resources: {str(error)}")

    # Link dynamic groups to policy (if needed in the future)
    for entitlement in entitlements_list:
        if "group" in entitlement and entitlement["deny"] == "false":
            current_group = groups_dict[entitlement["group"]]
            current_policy = actions_dict[entitlement["action"]]

            data = [current_group]
            dynamic_group_url = f"{api_url}/{current_policy}/permissionRule/dynamic"
            headers.update({"namespace": namespace_id})

            _log.info(f"Link the dynamic group {data} in the policy {current_policy}")
            try:
                r = requests.post(url=dynamic_group_url, data=json.dumps(data), auth=(plainid_user, plainid_password), headers=headers)
                if r.status_code == HTTPStatus.OK:
                    _log.info("Linked the dynamic group to the policy successfully")
                else:
                    _log.debug(f"Unexpected response code from REST call: {r.status_code}")
            except Exception as error:
                _log.error("Error linking dynamic group to policy: %s %s", str(error), sys.exc_info())
                raise error

    return True

# Utility Functions
# These functions were not changed, but they are key to the policy creation flow.
def get_resource_id_by_name(resource_name):
    """
    Retrieve resource ID by resource name.
    
    Args:
    - resource_name (str): The name of the resource.
    
    Returns:
    - str: The resource ID.
    """
    resource_obj = db_objects.get_object_by_name(resource_name, DYNAMIC_LIST_OBJ, base_url,
                                                 auth=(plainid_user, plainid_password))
    return resource_obj['id']

def get_action_by_name(action_name):
    """
    Retrieve action ID by action name.
    
    Args:
    - action_name (str): The name of the action.
    
    Returns:
    - dict: The action object.
    """
    action = db_objects.get_object_by_name(action_name, ACTION_OBJ, base_url,
                                           auth=(plainid_user, plainid_password))
    return action
```

### **Highlighted Changes**:

1. **`link_resources_to_policy`**:
   - **New Function**: This was added to link resources (created in the PlainID Asset Store) to the policies that control access to those resources. It sends a POST request to the API with the `policy_id` and a list of `resource_ids`.

2. **Changes in `create_policies_based_on_resource`**:
   - **Create Resource Types and Resources**: The logic to create resource types and resources was integrated using the `create_resource_type` and `create_resource` functions from `resources.py`. This ensures that for each RODAN resource, there is a corresponding resource in the PlainID Asset Store.
   - **Linking Resources to Policies**: The new `link_resources_to_policy` function was added to this workflow to link the newly created resources to the policies. This step is critical for applying the access controls defined by the policies to the actual resources.

### **Summary of Changes**:
- **Function Added**: `link_resources_to_policy` for linking resources to policies.

